# Layer 3 Adapters

This directory is reserved for optional project-specific side-effect adapters.

The base starter only posts fixtures to n8n, fetches executions, and writes reports. Add adapters here when a project must verify external systems after a live workflow run.
