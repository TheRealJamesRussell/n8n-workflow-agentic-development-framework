console.error('npm run setup is reserved for future placeholder replacement automation.');
console.error('For now, update manifest.json, workflow.json, and .env.development.example values manually.');
process.exit(1);
